<div class="container">
  <div class="text-center my-4">
    <h2>ユーザー一覧</h2>
    <div class="row">
      <% @users.each do |u| %>
      <div class=" card-deck col-sm-3">
        <div class="card my-3" style="width: 18rem">
          <%= image_tag  "#{u.thumbnail}", class: "card-img-top" %>
          <div class="card-body">
            <h5 class="card-title"><%=link_to "#{u.name}", user_path(u.id), method: :get, class: "EDITLink" %><h5>
                <p><%= "#{u.introduction}" %></p>
          </div>
        </div>
      </div>
      <% end %>
    </div>
    <%= paginate @users %>
  </div>
</div>